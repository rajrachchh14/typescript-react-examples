import * as React from "react";

const PropsExample = (props: { name: number }) => {
  return (
    <div>
      <h1>{props.name}</h1>
    </div>
  );
};
export default PropsExample;

// rafce
